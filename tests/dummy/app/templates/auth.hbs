{{#if session.currentUser}}
    <button {{action "signOut"}}>Sign Out</button>
{{else}}
    <button {{action "signIn" "anonymous"}}>Sign in anonymously</button>
    <button {{action "signIn" "twitter"}}>Sign in with Twitter</button>
    <button {{action "signIn" "github"}}>Sign in with GitHub</button>
    <button {{action "signIn" "facebook"}}>Sign in with Facebook</button>
    <button {{action "signIn" "google"}}>Sign in with Google</button>
{{/if}}

<div class="user-data">
    <h3>User Data</h3>
    <table class="user-data-table">
        <tr>
            <td class="bold">session.isAuthenticated</td>
            <td>
                {{#if session.isAuthenticated}}
                    {{session.isAuthenticated}}
                {{else}}
                    false
                {{/if}}
            </td>
        </tr>
        {{#if session.currentUser}}
            <tr>
                <td class="bold">session.provider</td>
                <td>
                    {{session.provider}}
                </td>
            </tr>
            <tr>
                <td class="bold">session.uid</td>
                <td>
                    {{session.uid}}
                </td>
            </tr>
        {{/if}}
    </table>
</div>
